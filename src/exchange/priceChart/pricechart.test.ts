// import { Trade } from '../trades';

// import { priceChartDataPoints } from './pricechart';

// test('Empty trades equals empty priceChartData', () => {
//   expect(priceChartDataPoints([], (d: Date) => d)).toEqual([]);
// });

test('hello', () => {
  expect(1 + 1).toEqual(2);
});

// // const trades: Trade[] {
// //   { baseAmount: }
// // }
// // test('parse simple trades', () => {
// //   expect(priceChartDataPoints([], (d: Date) => d)).toEqual([]);
// // };
